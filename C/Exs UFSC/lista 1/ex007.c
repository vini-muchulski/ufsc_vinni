#include <stdio.h>

int main(){

    int x,y;

    printf("Digite os valores de X e Y, resp: \n");
scanf("%d", &x);
scanf("%d", &y);

printf("%s", y!=0 ? "DIVISIVEL" : "INDETERMINADO");

return 0;
}